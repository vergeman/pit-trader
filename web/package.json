{
  "name": "web",
  "version": "1.0.0",
  "description": "Onnx runtime doesn't have any real operators, just runs an exported model",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
